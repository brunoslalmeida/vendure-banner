[ ] Make banner list items a button to open a left screen with the item list. This will allow the user to check them items without the need to move to another window. 
[ ] Create an button at the left screen to force expire a banner item. This allows the user to remove the banner from his website in a more simpler way.